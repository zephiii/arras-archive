When dealing with conflicts for mockups.json, always do accept incoming change. You will regenerate them later when you next run the server.
Also be aware that it will always show as an error, as its not JSON.